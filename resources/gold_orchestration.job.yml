# resources/gold_orchestration.job.yml
# ─────────────────────────────────────────────────────────────────────
# Gold Orchestration
# Gold pipeline
# ─────────────────────────────────────────────────────────────────────
resources:
  jobs:

    gold_orchestration:
      name: "SingleStore — Gold Pipeline"

      # ── Schedule (uncomment and adjust as needed) ─────────────────
      # schedule:
      #   quartz_cron_expression: "0 0 6 * * ?"   # Daily at 6:00 AM
      #   timezone_id: "America/New_York"
      #   pause_status: "UNPAUSED"

      # ── Notifications (uncomment to enable) ───────────────────────
      # email_notifications:
      #   on_failure:
      #     - yashasvi.palla@fenetwork.com

      # ── Retry / timeout (uncomment to enable) ─────────────────────
      # max_concurrent_runs: 1
      # timeout_seconds: 7200  # 2 hours

      tasks:

        - task_key: "run_gold_pipeline"
          pipeline_task:
            pipeline_id: ${resources.pipelines.customer_gold_pipeline.id}